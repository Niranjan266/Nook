<?xml version='1.0' encoding='utf-8'?>
<resources>
    <!--
      Kept only because the generated project referenced it, and something
      else may still. The adaptive icon uses @drawable/ic_launcher_background;
      this is the same night (#0E0D14), so anything still pointing here gets
      the right colour rather than the white it used to get.
    -->
    <color name="ic_launcher_background">#0E0D14</color>
</resources>
